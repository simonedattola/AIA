[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
markers =
    integration: tests requiring external services (live API and/or seeded MongoDB)
    unit: unit tests (default; no live HTTP server required)
filterwarnings =
    ignore::DeprecationWarning
addopts = -ra
