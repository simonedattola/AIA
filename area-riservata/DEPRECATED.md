# Deprecato

L'area riservata è integrata nel progetto principale:

- **Frontend:** `http://localhost:3000/area-riservata` (React, stesso sito)
- **API:** `http://localhost:8000/api/portal/*` (FastAPI + MongoDB)

Questa cartella Next.js (porta 3001, SQLite, seed fittizi) non è più usata in produzione.
Puoi rimuoverla dopo aver verificato il nuovo portale.
